export class AgentClasse {
    name: string;
    photoUrl: string;
    status: string;
  
    constructor(name: string, photoUrl: string, status: string) {
      this.name = name;
      this.photoUrl = photoUrl;
      this.status = status;
    }
  }